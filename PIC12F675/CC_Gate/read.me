Colour correction for PAL60 (or use the XTAL hack, which doesn't fix subcarrier for scaler use ..equal to DFO, only corrects freq) + Gate wire for the interim (haven't figured out software PWM, the chip lacks hardware PWM)
