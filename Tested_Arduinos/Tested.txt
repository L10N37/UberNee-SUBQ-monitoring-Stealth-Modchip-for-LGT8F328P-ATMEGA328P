Just a folder for tested arduinos, check images

The 168 version (This was sent by a seller but pictured to be the same as the old boot loader version) won't allow reflashing with anything connected to the 3.3v pin (i.e whilst installed in the console)
